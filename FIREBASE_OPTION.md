# Firebase option

The original Seed Seeker uses Firebase Hosting. This package defaults to GitHub
Pages because it requires no Firebase project or deployment token.

After the GitHub build is validated, the same generated `app/web/dist` folder
can be deployed to Firebase Hosting to obtain a `*.web.app` address.
