diff --git a/crates/seedfinder-core/src/main_world.rs b/crates/seedfinder-core/src/main_world.rs
--- a/crates/seedfinder-core/src/main_world.rs
+++ b/crates/seedfinder-core/src/main_world.rs
@@
+use crate::generator::{ARTIFACT_ITEMS, ArtifactKind};
+use crate::room::{RoomKind, SecretRoomKind};
+use crate::run::GeneratorCategory;
+
+/// Extra deterministic information used by Seed Scout only.
+///
+/// This deliberately does not live on `GeneratedWorld`: search creates large
+/// numbers of `GeneratedWorld`s and does not need a 24-floor metadata payload.
+#[derive(Clone, Debug, Eq, PartialEq)]
+pub struct ScoutFloorMetadata {
+    pub depth: u8,
+    pub secret_rooms: Vec<SecretRoomKind>,
+    pub artifacts: Vec<ArtifactKind>,
+}
+
+fn artifact_deck_snapshot(run: &RunState) -> Vec<f32> {
+    run.generator
+        .category(GeneratorCategory::Artifact)
+        .probabilities
+        .as_slice()
+        .to_vec()
+}
+
+fn generated_artifacts(before: &[f32], after: &[f32]) -> Vec<ArtifactKind> {
+    before
+        .iter()
+        .zip(after)
+        .zip(ARTIFACT_ITEMS)
+        .filter_map(|((&was, &now), artifact)| {
+            // The unique Artifact deck zeroes a class after it is drawn.
+            (was > 0.0 && now == 0.0).then_some(artifact)
+        })
+        .collect()
+}
+
+fn secret_room_kinds(rooms: &[crate::room::Room]) -> Vec<SecretRoomKind> {
+    rooms
+        .iter()
+        .filter_map(|room| match room.kind {
+            RoomKind::Secret(kind) => Some(kind),
+            _ => None,
+        })
+        .collect()
+}
+
+/// Replays the canonical main-dungeon generation pipeline and records metadata
+/// which the normal searchable `GeneratedWorld` intentionally omits.
+///
+/// No extra random draws are made: artifact discovery is based solely on
+/// before/after deck state.
+pub fn generate_scout_floor_metadata_with_challenges(
+    seed: DungeonSeed,
+    maximum_depth: u8,
+    challenges: Challenges,
+) -> Result<Vec<ScoutFloorMetadata>, MainWorldError> {
+    if !(1..=24).contains(&maximum_depth) {
+        return Err(MainWorldError::InvalidMaximumDepth(maximum_depth));
+    }
+
+    let target = effective_regular_depth(maximum_depth);
+    let dungeon_seed =
+        i64::try_from(seed.value()).expect("base-26 seed range fits Java long");
+    let roots = regular_depths(target)
+        .map(|depth| seed_for_depth(dungeon_seed, depth, 0))
+        .collect::<Vec<_>>();
+
+    let mut run = RunState::with_challenges(dungeon_seed, challenges);
+    let mut limited_drops = LimitedDrops::default();
+    let mut quests = QuestState::new();
+    let mut shop_run = ShopRunState::default();
+    let mut random = RandomStack::with_base_seed(0);
+    let mut output = Vec::with_capacity(usize::from(maximum_depth));
+
+    // Boss floors 5/10/15 do not enter `regular_depths`; add them below so the
+    // UI has one record per visible dungeon depth.
+    for (&root, depth) in roots.iter().zip(regular_depths(target)) {
+        random.push(root);
+        let artifacts_before = artifact_deck_snapshot(&run);
+
+        let rooms = match depth {
+            1..=4 => {
+                let floor = generate_sewer_floor(
+                    &mut run,
+                    &mut limited_drops,
+                    &mut quests,
+                    depth,
+                    &mut random,
+                )
+                .map_err(MainWorldError::Sewer)?;
+                floor.painted.rooms
+            }
+            6..=9 => {
+                let floor = generate_prison_floor(
+                    &mut run,
+                    &mut limited_drops,
+                    &mut quests,
+                    &mut shop_run,
+                    depth,
+                    &mut random,
+                )
+                .map_err(MainWorldError::Prison)?;
+                floor.painted.rooms
+            }
+            11..=14 => {
+                let floor = generate_caves_floor(
+                    &mut run,
+                    &mut limited_drops,
+                    &mut quests,
+                    &mut shop_run,
+                    depth,
+                    &mut random,
+                )
+                .map_err(MainWorldError::Caves)?;
+                floor.painted.rooms
+            }
+            16..=19 => {
+                let floor = generate_city_floor(
+                    &mut run,
+                    &mut limited_drops,
+                    &mut quests,
+                    &mut shop_run,
+                    depth,
+                    &mut random,
+                )
+                .map_err(MainWorldError::City)?;
+                floor.painted.rooms
+            }
+            20 => {
+                // Depth 20 is the city boss/Imp shop. It has no regular room
+                // graph, but the shop may consume an Artifact.
+                generate_city_boss_shop(&mut run, &mut shop_run, &mut random)
+                    .map_err(|error| {
+                        MainWorldError::Halls(HallsFloorError::BossShop(error))
+                    })?;
+                Vec::new()
+            }
+            _ => {
+                let floor = generate_halls_floor(
+                    &mut run,
+                    &mut limited_drops,
+                    &mut quests,
+                    &mut shop_run,
+                    depth,
+                    &mut random,
+                )
+                .map_err(MainWorldError::Halls)?;
+                floor.painted.rooms
+            }
+        };
+
+        let artifacts_after = artifact_deck_snapshot(&run);
+        random.pop();
+
+        output.push(ScoutFloorMetadata {
+            depth: u8::try_from(depth).expect("main-path depths fit u8"),
+            secret_rooms: secret_room_kinds(&rooms),
+            artifacts: generated_artifacts(&artifacts_before, &artifacts_after),
+        });
+    }
+
+    for boss_depth in [5_u8, 10, 15] {
+        if boss_depth <= maximum_depth {
+            output.push(ScoutFloorMetadata {
+                depth: boss_depth,
+                secret_rooms: Vec::new(),
+                artifacts: Vec::new(),
+            });
+        }
+    }
+
+    output.retain(|floor| floor.depth <= maximum_depth);
+    output.sort_by_key(|floor| floor.depth);
+    Ok(output)
+}

diff --git a/crates/seedfinder-wasm/src/lib.rs b/crates/seedfinder-wasm/src/lib.rs
--- a/crates/seedfinder-wasm/src/lib.rs
+++ b/crates/seedfinder-wasm/src/lib.rs
@@
 use shpd_seedfinder_core::main_world::{
-    CanonicalMainWorldGenerator, ConfiguredMainWorldGenerator, generate_main_world_with_challenges,
+    CanonicalMainWorldGenerator, ConfiguredMainWorldGenerator,
+    generate_main_world_with_challenges, generate_scout_floor_metadata_with_challenges,
 };
+use shpd_seedfinder_core::generator::ArtifactKind;
+use shpd_seedfinder_core::room::SecretRoomKind;
@@
 struct ScoutOutput {
     seed: SeedOutput,
     quests: Vec<ScoutQuestOutput>,
     items: Vec<ScoutItemOutput>,
+    floors: Vec<ScoutFloorOutput>,
     matched_requirements: usize,
     total_requirements: usize,
 }
+
+#[derive(Serialize)]
+#[serde(rename_all = "camelCase")]
+struct ScoutFloorOutput {
+    depth: u8,
+    secret_rooms: Vec<&'static str>,
+    artifacts: Vec<&'static str>,
+}
+
+const fn secret_room_id(kind: SecretRoomKind) -> &'static str {
+    match kind {
+        SecretRoomKind::Garden => "garden",
+        SecretRoomKind::Laboratory => "laboratory",
+        SecretRoomKind::Library => "library",
+        SecretRoomKind::Larder => "larder",
+        SecretRoomKind::Well => "well",
+        SecretRoomKind::Runestone => "runestone",
+        SecretRoomKind::Artillery => "artillery",
+        SecretRoomKind::ChestChasm => "chest_chasm",
+        SecretRoomKind::Honeypot => "honeypot",
+        SecretRoomKind::Hoard => "hoard",
+        SecretRoomKind::Maze => "maze",
+        SecretRoomKind::Summoning => "summoning",
+    }
+}
+
+const fn artifact_id(kind: ArtifactKind) -> &'static str {
+    match kind {
+        ArtifactKind::AlchemistsToolkit => "alchemists_toolkit",
+        ArtifactKind::ChaliceOfBlood => "chalice_of_blood",
+        ArtifactKind::CloakOfShadows => "cloak_of_shadows",
+        ArtifactKind::DriedRose => "dried_rose",
+        ArtifactKind::EtherealChains => "ethereal_chains",
+        ArtifactKind::HolyTome => "holy_tome",
+        ArtifactKind::HornOfPlenty => "horn_of_plenty",
+        ArtifactKind::MasterThievesArmband => "master_thieves_armband",
+        ArtifactKind::SandalsOfNature => "sandals_of_nature",
+        ArtifactKind::SkeletonKey => "skeleton_key",
+        ArtifactKind::TalismanOfForesight => "talisman_of_foresight",
+        ArtifactKind::TimekeepersHourglass => "timekeepers_hourglass",
+        ArtifactKind::UnstableSpellbook => "unstable_spellbook",
+    }
+}
@@
 fn scout_impl(request_json: &str) -> Result<String, String> {
@@
     let world = generate_main_world_with_challenges(seed, 24, challenges)
         .map_err(|error| format!("world generation failed: {error}"))?;
+    let floors = generate_scout_floor_metadata_with_challenges(seed, 24, challenges)
+        .map_err(|error| format!("scout metadata generation failed: {error}"))?
+        .into_iter()
+        .map(|floor| ScoutFloorOutput {
+            depth: floor.depth,
+            secret_rooms: floor.secret_rooms.into_iter().map(secret_room_id).collect(),
+            artifacts: floor.artifacts.into_iter().map(artifact_id).collect(),
+        })
+        .collect();
@@
     Ok(to_json(&ScoutOutput {
         seed: seed.into(),
         quests: scout_quest_outputs(world.quests),
         items,
+        floors,
         matched_requirements,
         total_requirements,
     }))
 }

diff --git a/web/src/lib/wasm/types.ts b/web/src/lib/wasm/types.ts
--- a/web/src/lib/wasm/types.ts
+++ b/web/src/lib/wasm/types.ts
@@
 export interface ScoutQuest {
   quest: QuestName
   variant: QuestVariant
   depth: number
 }
+
+export type SecretRoomName =
+  | 'garden'
+  | 'laboratory'
+  | 'library'
+  | 'larder'
+  | 'well'
+  | 'runestone'
+  | 'artillery'
+  | 'chest_chasm'
+  | 'honeypot'
+  | 'hoard'
+  | 'maze'
+  | 'summoning'
+
+export type ArtifactName =
+  | 'alchemists_toolkit'
+  | 'chalice_of_blood'
+  | 'cloak_of_shadows'
+  | 'dried_rose'
+  | 'ethereal_chains'
+  | 'holy_tome'
+  | 'horn_of_plenty'
+  | 'master_thieves_armband'
+  | 'sandals_of_nature'
+  | 'skeleton_key'
+  | 'talisman_of_foresight'
+  | 'timekeepers_hourglass'
+  | 'unstable_spellbook'
+
+export interface ScoutFloor {
+  depth: number
+  secretRooms: SecretRoomName[]
+  artifacts: ArtifactName[]
+}
@@
 export interface ScoutResult {
   seed: ParsedSeed
   items: ScoutItem[]
   quests: ScoutQuest[]
+  /** `?? []` in consumers keeps old cached worker responses compatible. */
+  floors?: ScoutFloor[]
   matchedRequirements: number
   totalRequirements: number
 }

diff --git a/web/src/designs/one/ScoutPanel.tsx b/web/src/designs/one/ScoutPanel.tsx
--- a/web/src/designs/one/ScoutPanel.tsx
+++ b/web/src/designs/one/ScoutPanel.tsx
@@
-import type { ScoutItem, ScoutResult } from '../../lib/wasm/types'
+import type {
+  ArtifactName,
+  ScoutFloor,
+  ScoutItem,
+  ScoutResult,
+  SecretRoomName,
+} from '../../lib/wasm/types'
@@
 const groupLetter = (group: number) => 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'[group % 26]
+
+const prettyToken = (value: string) =>
+  value
+    .split('_')
+    .map((word) => word.charAt(0).toUpperCase() + word.slice(1))
+    .join(' ')
+
+const secretRoomLabel = (room: SecretRoomName) => prettyToken(room)
+const artifactLabel = (artifact: ArtifactName) => prettyToken(artifact)
@@
   const floors = useMemo(() => {
     const byDepth = new Map<number, ScoutItem[]>()
     for (const item of result?.items ?? []) {
       byDepth.set(item.depth, [...(byDepth.get(item.depth) ?? []), item])
     }
-    return [...byDepth.entries()].sort(([left], [right]) => left - right)
+
+    // New Scout metadata contains all generated depths, including floors with
+    // no searchable equipment. Fall back to the historical item-only list for
+    // cached responses made by an older worker.
+    const metadata = result?.floors ?? []
+    if (metadata.length === 0) {
+      return [...byDepth.entries()]
+        .sort(([left], [right]) => left - right)
+        .map(([depth, items]) => ({ depth, items, metadata: undefined }))
+    }
+    return metadata
+      .map((floor) => ({
+        depth: floor.depth,
+        items: byDepth.get(floor.depth) ?? [],
+        metadata: floor,
+      }))
+      .sort((left, right) => left.depth - right.depth)
   }, [result])
@@
-                {result.items.length} item{result.items.length === 1 ? '' : 's'} across {floors.length} floor{floors.length === 1 ? '' : 's'}
+                {result.items.length} searchable item{result.items.length === 1 ? '' : 's'} · {floors.length} floor{floors.length === 1 ? '' : 's'} scouted
@@
-            {floors.map(([depth, items]) => {
+            {floors.map(({ depth, items, metadata }) => {
               const region = regionForDepth(depth)
               const quest = questByDepth.get(depth)
+              const secretCount = metadata?.secretRooms.length ?? 0
               return (
@@
                   </header>
+                  {metadata && (
+                    <div className="d1-floor-discovery">
+                      <div className="d1-floor-discovery-row">
+                        <span className="d1-floor-discovery-key">Secret rooms</span>
+                        <strong className={secretCount > 0 ? 'd1-secret-yes' : 'd1-secret-no'}>
+                          {secretCount > 0 ? `Yes (${secretCount})` : 'No (0)'}
+                        </strong>
+                        {secretCount > 0 && (
+                          <span className="d1-floor-discovery-detail">
+                            {metadata.secretRooms.map(secretRoomLabel).join(', ')}
+                          </span>
+                        )}
+                      </div>
+                      <div className="d1-floor-discovery-row">
+                        <span className="d1-floor-discovery-key">Artifacts</span>
+                        <strong>{metadata.artifacts.length || 'None'}</strong>
+                        {metadata.artifacts.length > 0 && (
+                          <span className="d1-floor-discovery-detail">
+                            {metadata.artifacts.map(artifactLabel).join(', ')}
+                          </span>
+                        )}
+                      </div>
+                    </div>
+                  )}
                   <ul className="d1-item-list">

diff --git a/web/src/designs/one/styles.css b/web/src/designs/one/styles.css
--- a/web/src/designs/one/styles.css
+++ b/web/src/designs/one/styles.css
@@
+.d1-floor-discovery {
+  display: grid;
+  gap: 4px;
+  padding: 8px 12px;
+  border-bottom: 1px solid var(--line);
+  background: color-mix(in srgb, var(--region) 6%, transparent);
+  font-size: 12px;
+}
+
+.d1-floor-discovery-row {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 6px 10px;
+  align-items: baseline;
+}
+
+.d1-floor-discovery-key {
+  min-width: 82px;
+  color: var(--muted);
+}
+
+.d1-floor-discovery-detail {
+  color: var(--muted);
+}
+
+.d1-secret-yes {
+  color: var(--success);
+}
+
+.d1-secret-no {
+  color: var(--muted);
+}
